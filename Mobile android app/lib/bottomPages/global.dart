import 'package:flutter/material.dart';

class GlobalClass extends StatelessWidget {
  const GlobalClass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}